# Large data files (keep scripts, not raw data — everything in data/ is reproducible)
data/

# Scratch / working files (agent-extracted filing text, throwaway)
tmp/

# Python
__pycache__/
*.pyc
.venv/

# Local env / secrets
.env
.env.local

# OS
.DS_Store
Thumbs.db
